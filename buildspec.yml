version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - echo Instalando dependencias...
      - npm install
  build:
    commands:
      - echo Compilando el proyecto...
      - npm run build
artifacts:
  files:
    - appspec.yml
    - scripts/**
    - dist/**
